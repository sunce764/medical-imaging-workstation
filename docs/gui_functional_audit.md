# GUI 功能与交互审计

本记录维护 2026-09-04 全功能审计的范围、复现与验证结果；缺陷修复历史统一见
[CHANGELOG](../CHANGELOG.md)。开始基线：`ad99a10b8bb1827c4c696f38c831e19244182e89`。

## 当前状态

本轮已完成下表列出的功能、错误分支与交互组合审计，并修复实际复现的问题。
这是基于上述 commit 的本地候选验证记录；不代表已经 push 或取得远端 CI。
Git / GitHub Actions 状态须另查精确提交 SHA，不能用历史 badge 代替。

本地完整回归 **1121/1121**、数据无关子集 **1010/1010**（均 exit 0）；
完整回归行覆盖 **3974/4253，93.44%**。
实际 CPU AI、矩阵后端和原生 macOS 窗口另行验证，不混入自动测试计数。
机器可读结果及候选源码 / 模型指纹见 [证据 JSON](gui_functional_audit_evidence.json)。
这些结果不等于穷尽所有输入、硬件和操作组合，也不证明不存在其他 BUG。

## 检查范围

| 功能 | 已验证的范围 | 验证层 |
|---|---|---|
| DICOM 加载 | 空目录、坏片、混合序列/尺寸、排序、HU/显示预览、独立几何能力、失败回滚 | 完整 / 子集回归；原始 RIDER 233 层实载 |
| 阅片 | 六种窗、独立窗、右拖、反色、缩放/平移、切片、Cine、单/双/四窗 | 回归；原生按钮点击与实际像素 |
| MPR / 厚层 | 三平面坐标、方向/比例、滚轮联动、MIP/MinIP/AIP、模式往返 | 合成已知值 / 真实序列回归 |
| 标注 | 卡尺、自由画笔、椭圆 ROI、矩形/套索截取、删除、全局/单层、交互中换工具/切片 | 真实 Qt 鼠标事件和持久化状态断言 |
| 分割 | 画笔/橡皮、标签/半径、追踪、撤销、图例、清空/重置与后台回调竞态 | 合成标签回归；延迟成功 / 失败 / 进度回调 |
| AI / 缓存 | 成功、失败、降级、进度、取消、过期回调、缓存身份/轴向/几何 | 回归；一次实际 CPU ONNX 整卷推理 |
| 定量 / 三维 | 数值、置信度、单位限制、CSV、模型卡、mesh 旋转/形状/STL | 数值回归；原生弹窗 / 旋转 / 临时 STL 导出 |
| 保存 / 导出 | 工程往返、原子性、取消、写入失败、脱敏、路径安全 | 临时目录回归；PNG 实际写失败 |
| 随访 | 加载失败、进入/退出、层对应、配准/拒绝、差值显示、序列/布局切换 | 合成及现有真实数据回归；非独立临床随访验证 |
| 重建 | 模体/原图来源、弦图、BP/FBP/DFR/DMR/ART/SIRT/ASD-POCS/DL、参数、取消、缓存失效 | 数值 / 调度回归；原生 BP/FBP/DFR；实际 16² 矩阵后端；本机 DL 权重推理 |
| UI 状态 | 空载可用性、语言、尺寸、暗色面板、弹窗、长状态文字 | 原生 1280×800 / 1600×950 中英文；两种系统 palette 回归 |

## 本轮复现与修复

- **AI 与手动操作冲突**：重置没有取消旧任务，晚到结果会重新填满清空的蒙版；
  手动画笔也会被晚到 AI 覆盖。重置和有效手动编辑现在使旧任务代次失效，编辑接管后保留停止原因。
  重置非空蒙版也保留明确清空的保存意图。
- **绘制上下文错位**：MPR 悬停重绘移除了未完成笔划，切层后松手可能把旧坐标存到新层。
  普通刷新保留预览；换层、工具、平面、序列、模式和重置取消未完成操作。
  非 Axial 视图在落笔前说明限制，避免画完才丢弃。
- **重建来源残留**：新序列恰好同层数、同当前层号时，旧弦图、BP 缓存和结果仍可使用。
  统一来源失效处理；卸下最后一个模体时清空 V1；空载生成弦图入口禁用。
- **显示路径不一致**：未知 HU 单位 CT 的重建参考图没有应用阅片页预览变换，切页后突然泛白。
  两个显示入口现在共用切片变换，原始数组和 HU 分析权限不变。
- **复位与入口状态**：复位遗漏反色、厚层投影和旧探针读数；空载 Cine 可点；
  套索统计入口与矩形 / ROI 的 HU 条件不一致。相关控件状态已统一，套索工具提示也与实际截取功能一致。
- **导出结果不实**：PNG 保存返回 false 后仍可能显示“图像已保存”。现在检查真实返回值并报告失败。
- **深色系统弹窗**：模型卡 / 三维窗口曾假定系统总是浅色，macOS 深色模式下正文对比度仅约 1.16:1。
  显式统一背景和文字色；深 / 浅系统 palette 下三维普通文字最低 6.68:1，模型卡普通文字 10.24:1。
  原生截图也已目视复核；测试不把 HTML 内每种强调色都计入该数值。
- **长状态被截断**：AI 结果状态未换行；重建耗时固定一行导致取消说明看不全。现已允许完整换行。

新增 / 扩展回归集中在 `tests/test_gui.py` 中的 `test_full_ui_state_audit`、
`test_drawing_context_audit`、`test_export_failure_and_status_audit`、
`test_ct_window_preview_without_hu`、`test_mesh_dialog_text_is_readable`。
这些入口在完整和 `SKIP_REAL_DATA=1` 两层均执行。

## 验证过程

所有 Python 命令均在 `conda activate dicom_gui` 后运行。测试数量为断言计数。

| 证据 | 实测结果 |
|---|---|
| 首轮状态缺陷回归 `/tmp/gui-0904-audit-red.log` | 19 项中 16 项失败，exit 1；修复后 19/19 通过 |
| 第二轮显示 / 绘制回归 `/tmp/gui-0904-audit-round2-red.log` | 32 项中 2 项失败，exit 1；最终全套均通过 |
| 深色弹窗回归 `/tmp/gui-0904-audit-dialog-red.log` | 13 项中 2 项失败，exit 1；最终深浅背景全部通过 |
| 取消状态排版 `/tmp/gui-0904-audit-status-red.log` | 4 项中 1 项失败，exit 1；最终全套通过 |
| `COVERAGE_FILE=/tmp/gui-0904-audit-final.coverage python -m coverage run tests/test_gui.py` | 1121/1121，exit 0；日志 `/tmp/gui-0904-audit-full-final.log` |
| `SKIP_REAL_DATA=1 python tests/test_gui.py` | 1010/1010，exit 0；日志 `/tmp/gui-0904-audit-subset-final.log`；本机已有外部 DL 权重，数量不等于无权重 CI |
| `ruff check .` / `git diff --check` | exit 0 |

实际 AI 使用既有 `RIDER_HU_declared` 副本：先检查缓存，因缺少轴向契约拒绝恢复；
随后走真实 CPU 模型和 Qt 回调，蒙版 / confidence 均为 233×512×512，18 个非背景标签，
没有降级，定量和导出入口就绪。这是功能验证，不是独立 HU 标定证明或分割准确率验证。
仅执行一次，未保存蒙版；机器配置、耗时、峰值内存、模型 SHA 和当时源码指纹见证据 JSON。
该次运行后仅继续修改显示 / 弹窗等 UI 路径，不重复昂贵推理。

矩阵后端使用临时矩阵缓存，实际运行 16² / 180 角度的 DMR、ART、SIRT、ASD-POCS，
四种均得到有限矩阵和有限图像；不声称穷尽 32² / 64² 的所有参数组合。
原生 `cocoa` 窗口 13 项断言通过，另检查截图：临床双语两种尺寸、重建图、模型卡、三维窗口。

## 证据与限制

- 自动化基线：`/tmp/gui-0904-audit-baseline.log`；覆盖率：`/tmp/gui-0904-audit-coverage.json`。
- 状态复现探针：`/tmp/gui-0904-audit-probes.py` 和同名 `.log`，使用临时合成 DICOM。
- 原始数据、既有导出/缓存、模型、实验结果和已提交 PDF 保持原样。
- 原生测试脚本 / 日志：`/tmp/gui-0904-audit-native.py`、同名 `.log`；截图前缀 `/tmp/gui-0904-native-`。
- 临时路径可能被系统清理；关键摘要与指纹已写入本记录和证据 JSON，完整运行日志仅在本机。
