# ============================================================
# 患者数据（PHI）—— 绝不入库
# ============================================================
肺癌/
Exported_Lesions/
*.dcm

# ============================================================
# 大文件 / 模型外部权重（119MB，需单独获取，见 README）
# ============================================================
models/organs.onnx.data
organs.onnx.data
# 学习式重建模型的外部权重（7.7MB）。与 organs.onnx 同一套约定：仓库只提交 .onnx 图，
# 权重由 `python experiments/recon_dl.py matrix && ... export` 以固定种子重建。
# 注意 ONNX 按【相对 .onnx 的路径】解析 .data，两者必须同目录。
models/recon_dl_v*.onnx.data

# ============================================================
# 缓存与生成物
# ============================================================
__pycache__/
*.pyc
*.pyo
.matrix_cache/
.DS_Store
.coverage
htmlcov/
.ruff_cache/

# ============================================================
# 私人法律文件（含个人姓名/身份信息）—— 绝不入库（仓库将来会公开）
# ============================================================
coop_agreement*
docs/coop_agreement*
*合作开发协议*
签章页*
*签章页*

# ============================================================
# 外部数据集 / 推理缓存（如 experiments 下载的 TotalSegmentator nii、pred 缓存）—— 绝不入库
# ============================================================
*.nii
*.nii.gz
*.npy
*.npz

# ============================================================
# 本地编辑器 / 工具配置
# ============================================================
.vscode/
.cursor/
.continue/
.claude/

# ============================================================
# 本地 AI 工作上下文 —— 含「当前卡点/导师姓名」，仓库将公开，保持本地不入库
# ============================================================
CLAUDE.md

# 研究三的训练权重（每个约 7.6MB，5 档共约 38MB）。产物目录只提交图表与 CSV；
# 权重可由 `python experiments/recon_dl.py matrix` 以固定种子完整重建，无需入库。
experiments/results/recon_dl_w*.pt
# 研究四曾试训一个轻量学生模型以量化「压缩换 Dice」的代价，该方向已撤（本机无 GPU，
# 教师基线与推理成本两条线已各自闭合，学生线不是证据链的缺口）。训练与评估脚本已删，
# 下面两条只为拦住本地残留的冒烟产物——它们只训了 1 epoch，不代表任何结论。
experiments/results/seg3d_w*.pt
experiments/results/seg3d_student_*

# 研究四的公开数据本地缓存（TotalSegmentator-CT-Lite 子集，约 2GB）。
# 不入库：外部数据一律不随仓库分发；可由 `python experiments/seg3d_data.py fetch`
# 从固定 commit 重新抓取，清单里的 SHA256 可验证拿到的是否逐字节相同。
experiments/.seg3d_cache/
experiments/.seg3d_prep/

.agents/
.codex/
AGENTS.md
