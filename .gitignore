# ============================================================
# 患者数据（PHI）—— 绝不入库
# ============================================================
肺癌/
Exported_Lesions/
*.dcm

# ============================================================
# 大文件 / 模型外部权重（119MB，需单独获取，见 README）
# ============================================================
models/organs.onnx.data
organs.onnx.data
# 学习式重建模型的外部权重（7.7MB）。与 organs.onnx 同一套约定：仓库只提交 .onnx 图，
# 权重可由 `python experiments/recon_dl.py matrix && ... export` 按固定配置重新训练导出。
# 【不是 byte-identical 重建】研究三的历史权重产出于 train_one 固定 PyTorch RNG **之前**。
# 重训会产生新权重；byte-level 与 functional equivalence 均未验证。
# （「功能等价」曾被写在这里，但本项目从未做过等价性测量——没有对新旧权重跑过同一
#   评测集的对照。没测过的事不写成结论。）
# 注意 ONNX 按【相对 .onnx 的路径】解析 .data，两者必须同目录。
models/recon_dl_v*.onnx.data
# 通配兜底：上面两条只匹配当下存在的确切文件名。新增/改名任何模型，其外部
# 权重块都会是多 MB 的 .onnx.data，实测 `git check-ignore models/seg3d_v1.onnx.data`
# 在只有具名规则时是 NOT-IGNORED，故加通配。
*.onnx.data

# ============================================================
# 缓存与生成物
# ============================================================
__pycache__/
*.pyc
*.pyo
.matrix_cache/
.DS_Store
.coverage
htmlcov/
.ruff_cache/

# ============================================================
# 私人法律文件（含个人姓名/身份信息）—— 绝不入库（仓库已公开）
# ============================================================
coop_agreement*
docs/coop_agreement*
*合作开发协议*
签章页*
*签章页*

# ============================================================
# 外部数据集 / 推理缓存（如 experiments 下载的 TotalSegmentator nii、pred 缓存）—— 绝不入库
# ============================================================
*.nii
*.nii.gz
*.npy
# 其他医学影像格式（西门子 raw、MetaImage、NRRD、HDF5）——目前仓库里没有，
# 但任何一份掉进工作区都不该入库。
*.ima
*.IMA
*.dicom
*.mhd
*.mha
*.nrrd
*.h5
*.npz

# ============================================================
# 本地编辑器 / 工具配置
# ============================================================
.vscode/
.cursor/
.continue/
.claude/

# ============================================================
# 本地开发上下文（个人工作笔记，与软件本身无关），保持本地不入库
# ============================================================
CLAUDE.md

# 研究三的训练权重（每个约 7.6MB，5 档共约 38MB）。产物目录只提交图表与 CSV；
# 权重可按固定配置重新训练，无需入库；但同上，历史产物早于 RNG 固定：重训会产生新权重，
# byte-level 与 functional equivalence 均未验证。
experiments/results/recon_dl_w*.pt
# 研究四的 3D 分割权重，同理不入库（可按固定种子与固定 commit 的数据**重新训练**；
# 训练本身已固定 RNG，但仍不对 byte-identical checkpoint 作保证）
experiments/results/seg3d_w*.pt
# 同理兜底：训练权重写到别的目录或别的文件名一样不该入库。
*.pt
*.pth
*.ckpt

# 研究四的公开数据本地缓存（TotalSegmentator-CT-Lite 子集，约 2GB）。
# 不入库：外部数据一律不随仓库分发；可由 `python experiments/seg3d_data.py fetch`
# 从固定 commit 重新抓取，清单里的 SHA256 可验证拿到的是否逐字节相同。
experiments/.seg3d_cache/
experiments/.seg3d_prep/

.agents/
.codex/
AGENTS.md
experiments/results/seg3d_student_ch8.*

# 由 `python tools/declare_rider_hu.py` 生成的 RIDER 派生副本（约 117MB）。副本只比
# 源多一个 explicit `RescaleType=HU` tag，ImageType 与 PixelData 逐字节不变。
# 与 `肺癌/` 同理不入库；`*.dcm` 已覆盖其中的文件，此处按仓库惯例再加目录兜底，
# 以防副本里出现非 .dcm 文件。
RIDER_HU_declared/
